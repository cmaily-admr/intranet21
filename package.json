{
  "name": "intranet21",
  "version": "1.0.0",
  "private": true,
  "description": "Espace salariés — ADMR Côte-d'Or",
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve --port=8080",
    "clean": "rimraf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "rimraf": "^5.0.5"
  }
}
