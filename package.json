# À la racine du projet
cat > package.json << 'EOF'
{
  "name": "intranet21",
  "version": "1.0.0",
  "description": "Intranet21",
  "main": "index.js",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "dependencies": {}
}
EOF

git add package.json
git commit -m "Add package.json"
git push
